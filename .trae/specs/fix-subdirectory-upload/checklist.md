# Checklist

## 子目录上传
- [ ] 上传 `./client/test_delete/` 目录中的文件时，服务器不再拒绝
- [ ] 服务器端 `mkdir_p()` 递归创建目录函数已实现
- [ ] `worker_handle_put()` 在 `open()` 前调用 `mkdir_p()` 创建父目录
- [ ] 上传根目录文件（如 `haha.txt`）仍正常工作
- [ ] 上传一级子目录文件（如 `test_delete/haha.txt`）正常工作
- [ ] 上传多级子目录文件（如 `a/b/c.txt`）正常工作
- [ ] 上传完成后服务器 `./copy/` 下正确保留子目录结构

## 编译
- [ ] 代码审查通过：所有函数引用、头文件包含正确
- [ ] 服务端编译通过无错误
- [ ] 无新增编译警告
